# (wired-in) web

This project was bootstrapped with [Create React App] and will b

## Available Scripts

In the project directory, you can run:
### `npm start`
### `npm test`
### `npm run build'
### `npm run eject`
